# T016: actual Enterprise application delivery

Passed on 2026-09-05 against runtime commit `214a805b7bce5f6f6e95c5543f1351f7dacbf8f9`. This follow-up changes validation records only. It closes the missing ordinary application path test; it does not turn the older shared-stream or isolated publisher-to-send results into passing evidence.

## Runtime and isolation

The existing port-3653 Next process, PID 122926, remained running in this worktree's `server` directory. A second `node node_modules/next/dist/bin/next dev` process ran from the same directory. It inherited the original process environment and loaded the same `server/.env.local`, including `APP_EDITION=enterprise` and `NEXT_PUBLIC_EDITION=enterprise`. No `EDITION` override or `E2E_SKIP_APP_INIT` was supplied. The temporary overrides were:

```text
PORT=3654
NEXTAUTH_URL=http://localhost:3654
NEXT_DIST_DIR=.next-t016
REDIS_EVENT_STREAM_PREFIX=t016-20260905-event-stream:
REDIS_EVENT_CONSUMER_GROUP=t016-20260905-processors
```

The PostgreSQL database, real filesystem storage, Redis server and GreenMail provider stayed unchanged. Only event routing was isolated. The new process used the normal Next instrumentation hook, `initializeApp`, subscriber registration and email service initialization. Its final consumer was `consumer-543133`; `/proc/543133/cwd` identifies this branch. Redis MONITOR associates its `XREADGROUP` and `XACK` commands with connection `172.19.0.1:37494`. Older startup attempts left idle consumers with no pending events; no other worktree consumed this namespace.

Initial separate builds exhausted worktree filesystem space. The prior production webpack cache was moved to `/tmp/t016-prior-production-webpack-cache`, preserving it. The smoke build moved to `/tmp/t016-worktree/server/.next-fresh`, with dependency-directory symlinks preserving module resolution. A fresh restart then initialized the real email services. No running dev cache was removed. The recorded acceptance used this final startup, not the failed setup attempts.

Enterprise startup logged the existing unreachable Temporal endpoint and missing credential-vault encryption key. Event bus, subscriber, accumulator, limiter and retry queue had initialized before these unrelated errors. This is actual Enterprise SMTP runtime coverage, not live Temporal scheduling or paid-provider coverage.

## UI actions and results

Used only the existing synthetic ticket `ATT-SMOKE-fcfd3bf2` (`fcfd3bf2-29b5-450d-8f95-a69adeabd0ea`) in synthetic tenant `dd8cb218-d46d-47f3-be27-8aa50aad5fce`. Its original null assignment was temporarily set to the synthetic MSP user to receive the portal reply. No customer records or recipients were used.

1. Signed in to the MSP UI, opened the ticket, selected Add Comment, and used its actual file input and Send button to submit a public PDF. The file input was populated with a browser `File`/`DataTransfer` change event; application handlers were not invoked directly.
2. Captured the server-owned draft row before submission, then the attached document, correct ticket association, public comment, persisted publication payload/stable event ID and dispatched timestamp afterward.
3. Submitted a separate internal PDF comment, then uploaded and canceled another PDF through the composer. The internal comment committed as internal. Cancellation changed its attachment to `removed`, with null comment and no publication event.
4. Switched to the portal test identity and clicked Reply on the MSP acceptance comment. Uploaded a different one-page PDF through that reply's actual file input and clicked Reply. Captured draft/commit state, author, parent comment and thread association.
5. Captured real Redis stream entries, consumption/ACKs, real storage bytes, recipient ledgers and GreenMail raw MIME. Decoded MIME attachments with `mailparser` and compared SHA-256 against both the original PDFs and persisted storage files. `pdfinfo` confirms both acceptance PDFs have one page.
6. Reinserted the two original email event envelopes into the same real Redis stream with `XADD`, preserving event IDs. Both replay entries were consumed and acknowledged without additional emails or increased delivery attempts. This replay checks event deduplication; it does not simulate a provider retry failure.

| Result | MSP public comment | Portal reply |
| --- | --- | --- |
| Comment | `7bf3ef5c-86a8-4915-873d-16eccff34936` | `ba69e15e-1cab-44ed-9bbf-43ac8b89e909` |
| Event | `edb00a68-829e-4725-8298-87849f9fff6a` | `e064bad2-baaa-4b05-8807-86dc34191ab2` |
| Original Redis ID | `1788574847376-0` | `1788574986858-0` |
| Replay Redis ID | `1788575035140-0` | `1788575035140-1` |
| Recipient | `attachment-recipient@example.test` | `attachment-smoke-2134722f@example.test` |
| File | `t016-msp-valid.pdf` | `t016-portal.pdf` |
| Bytes | 638 | 635 |
| SHA-256 | `b9ef4c34b1c23875b13ddbfd92fd77177786077e8a046d0c969635364c97f282` | `b74b8ade3cfa9e46ea36e49d2f3050b145c40fe943feeac2b03b84bdef161f9a` |
| Delivery count / ledger | 1 / `sent`, attempts 1 | 1 / `sent`, attempts 1 |

The reply's parent is exactly the MSP comment above. Each MIME message has one attachment: its own original file. The internal comment `04ac0a4c-6e10-46f9-9336-317235eb53ec` was consumed but produced no test-recipient mail or attachment ledger entry. Canceled attachment `cc411bec-9c37-4bd1-809c-fc8a5153d9b3` stayed removed/unclaimed. Neither file appeared in portal ticket content or any captured test email. Existing unrelated comment files on this ticket were also absent from the acceptance emails.

A preliminary UI submission used a 96-byte PDF stub before the fixture was inspected. It also delivered once, but is explicitly excluded from the two valid-PDF acceptance results. Its separate comment, MIME and original hash are retained in the local evidence and were included in cleanup.

## Evidence and cleanup

Durable identity/hash/count results are in [T016-evidence.json](T016-evidence.json). Full local evidence is at `/tmp/alga-smoke-evidence/ticket-comment-attachments-t016-20260905`:

- `application-runtime.log`, `startup.json`, `consumer-identity.json`: actual final startup, edition source and routing.
- `redis.json`, `redis-consumption.jsonl`, `replays.json`: original events and real consumption/replay ACK evidence.
- `msp-valid-draft.json`, `portal-draft.json`, `final-db.json`, `storage.json`: lifecycle, publication, exact ownership, stored bytes and recipient results.
- `mail-*.eml`, `mime-report.json`, original PDFs and `*-pdfinfo.txt`: independently decoded original MIME and PDF validity.
- `t016-*.png`, `portal-visible.json`: actual UI state and internal/canceled exclusion.
- `verify.cjs`, `acceptance.json`: assertions joining the observations above; no mocked publisher/subscriber/storage/provider seams.
- `cleanup.json`, `after-cleanup.json`, `redis-cleanup.json`, `mail-cleanup.json`, `restoration.json`: scoped removal and environment restoration.

After the final count check, deleted only this run's five exclusively owned documents/files and four comments, with shared-document/thread guards. Removed their reply tokens, delivery rows, reactions and associations; restored the original null ticket assignment. Deleted only the three captured smoke messages from GreenMail after archiving their MIME. Stopped only the temporary app process, removed its exclusive Redis namespace and build-output symlink, and returned browser panes to port 3653. Original environment files, package-lock changes and migration-CLI changes remain unchanged.

The server typecheck and 51 focused attachment/storage/UI regressions passed again. Their opt-in PgBoss recovery-to-SMTP scenario retains its documented test seams and is separate from this actual UI acceptance. Package builds/typechecks and the production Next build passed on the unchanged runtime source in the preceding revision; no new production build is claimed for this documentation-only follow-up. Live Graph/Resend, live Temporal and Citus remain unverified.

The old default namespace still mixes worktrees. Its earlier text-only mail was consumed by a different version and is an environment-routing issue. This run proves the requested branch behavior under matching-edition routing isolation without disrupting that shared namespace or resending the original historical event.
